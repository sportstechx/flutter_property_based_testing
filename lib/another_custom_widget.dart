import 'package:flutter/material.dart';

class AnotherCustomWidget extends StatelessWidget {
  const AnotherCustomWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
